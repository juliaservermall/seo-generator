# ❓ FAQ и примеры

## Вопросы про начало работы

### В: Где взять OpenAI API ключ?

**О:** 
1. Откройте https://platform.openai.com/account/api-keys
2. Авторизуйтесь (или создайте аккаунт, если его нет)
3. Нажмите кнопку "Create new secret key"
4. Скопируйте появившийся ключ (вы больше его не сможете увидеть!)
5. Вставьте в `.env` файл проекта

**Важно**: ключ начинается с `sk-` и содержит буквы, цифры, дефисы.

---

### В: Сколько денег потребуется на OpenAI?

**О:** Зависит от объёма:
- **gpt-5.4-nano** (самая дешёвая модель): примерно $0.01 за 100 строк описаний
- **gpt-5.4-mini**: примерно $0.03 за 100 строк
- **gpt-5.4**: примерно $0.30 за 100 строк

Проверить расход: https://platform.openai.com/account/billing/overview

---

### В: Какие версии Node.js нужны?

**О:** Node.js 18 или выше. Проверить версию:
```bash
node --version
```

Если версия старше — скачайте новую с https://nodejs.org (выберите LTS версию).

---

## Вопросы про файлы

### В: Какой максимальный размер файла?

**О:** По умолчанию 50 МБ. Можно изменить в `.env`:
```
MAX_UPLOAD_MB=100
```

---

### В: Какие форматы поддерживаются?

**О:** CSV и XLSX (Excel). 

Требования к файлу:
- **Обязательно** наличие колонки с названием типа "Наименование" или "Наименование элемента"
- **Обязательно** наличие колонки, содержащей слово "раздел" (например "Название раздела")
- **Обязательно** наличие колонки, содержащей слово "описание" или "описания"

---

### В: Сохранит ли приложение мой исходный файл в исходном формате?

**О:** Да! Полностью:
- CSV скачивается как CSV (с теми же разделителем `;`, BOM, кодировкой)
- XLSX скачивается как XLSX (все формулы, стили, другие колонки сохранены)

Изменяется **только** выбранная вами колонка описания. Остальное нетронутое.

---

### В: Что делать, если "Обязательные колонки не найдены"?

**О:** Ошибка означает, что в файле отсутствуют нужные колонки. Проверьте:

1. **Колонка наименования**: должна называться одним из:
   - "Наименование"
   - "Наименования"
   - "Наименование элемента"
   - "Наименование элементов"

   Если её нет — добавьте в Excel и сохраните файл.

2. **Колонка раздела**: должна содержать слово "раздел" (любой регистр)
   - Примеры: "Раздел", "Название раздела", "Название раздела {ISECT_NAME}"
   - Если её нет — добавьте и сохраните.

3. **Колонка описания**: должна содержать слово "описание" или "описания"
   - Примеры: "Описание", "Описание для анонса", "Детальное описание"
   - Если её нет — добавьте и сохраните.

---

## Вопросы про генерацию

### В: Почему генерация идёт медленно?

**О:** Возможные причины:
1. **Сервер OpenAI перегружен** — в пиковые часы API медленнее
2. **Большой объём контекста** — если вы выбрали много колонок, каждый запрос больше
3. **Медленное соединение** — проверьте интернет
4. **Бесплатная версия OpenAI** — имеет лимиты на частоту запросов

**Решение**:
- Используйте модель `gpt-5.4-nano` вместо `gpt-5.4` (в 10 раз быстрее)
- Выбирайте только необходимые колонки для контекста
- Попробуйте в другое время

---

### В: Можно ли остановить генерацию посередине?

**О:** Да! Нажмите кнопку "Остановить". Уже сгенерированные описания сохранятся, остальные пропустятся. Вы сможете скачать результат с частично заполненной колонкой.

---

### В: Что означает статус "пропущено"?

**О:** Строка была пропущена по одной из причин:
- **"Целевая колонка уже заполнена"** — вы отметили чекбокс "Пропускать заполненные"
- **"Пустое наименование"** — в строке нет значения в колонке наименования

---

### В: Почему некоторые строки вернули ошибку?

**О:** Возможные причины:
1. **OpenAI API ошибка** — неверный ключ, закончился баланс, API в downtime
2. **Проблема с URL** — если вы выбрали колонку с URL и приложение пыталось его загрузить, но ссылка была неживой (таймаут)
3. **Проблема с интернетом** — временный сбой

Приложение не ломается на ошибке одной строки — продолжает обработку остальных. Попробуйте:
- Остановить и запустить генерацию заново для ошибочных строк
- Проверить API ключ и баланс
- Заново загрузить файл и попробить ещё раз

---

### В: Может ли приложение загружать содержимое со внешних ссылок?

**О:** Да, если в выбранной колонке есть URL (http/https):
- Приложение загрузит страницу или текстовый файл
- Извлечёт текст (до 8000 символов)
- Подмешает его в контекст для генерации

Это помогает, если у вас в файле есть колонка с ссылкой на описание товара, и вы хотите, чтобы модель учла информацию со страницы.

**Ограничения**:
- PDF/DOCX/XLSX файлы по URL не поддерживаются (ошибка)
- Таймаут загрузки — 10 секунд

---

## Вопросы про HTML

### В: Что отличается в режиме HTML?

**О:** 
- **Без HTML** (галочка выключена): обычный текст, абзацы разделены пустой строкой
- **С HTML** (галочка включена): результат в виде HTML-разметки с тегами `<p>`, `<ul>`, `<li>`, `<b>`, `<table>` и т.д.

**Пример без HTML:**
```
Отличный сервер для графики.

Быстрая память, мощный GPU.
```

**Пример с HTML:**
```html
<p>Отличный сервер для графики.</p>
<p>Быстрая память, мощный GPU.</p>
```

---

### В: Какие HTML теги разрешены?

**О:** 
```
div, p, h2, h3, ul, ol, li, table, tbody, tr, td, b, strong, br
```

**Запрещены**: `script`, `style`, `a`, `img`, `span`, `onclick`, `class`, `style=` и т.д.

Если модель вернула с недопустимыми тегами — приложение их автоматически удалит.

---

### В: Можно ли вставить в HTML свои стили (CSS)?

**О:** Нет. Стили запрещены по соображениям безопасности. HTML — только разметка, без CSS.

Если вам нужны стили — вставьте результат в Excel или вёб-страницу после генерации.

---

## Вопросы про модели OpenAI

### В: Какую модель выбрать?

**О:**
| Модель | Скорость | Качество | Цена | Когда использовать |
|--------|----------|----------|------|--------------------|
| gpt-5.4-nano | 🟢 Быстро | 🟡 Хорошо | 💰 Дешёво | Для быстрого теста, много строк |
| gpt-5.4-mini | 🟡 Норм | 🟢 Отлично | 💰 Норм | Баланс скорости и качества |
| gpt-5.4 | 🔴 Медленно | 🟢 Лучшее | 💸 Дорого | Для критичных описаний |

По умолчанию рекомендуется `gpt-5.4-nano` для больших объёмов, `gpt-5.4-mini` если нужно качество.

---

### В: Как добавить свою модель?

**О:** Отредактируйте файл `lib/models.ts`:

```typescript
export const AVAILABLE_MODELS: ModelOption[] = [
  { id: "gpt-5.4-nano", label: "gpt-5.4-nano (быстрая)" },
  { id: "gpt-4-turbo", label: "gpt-4-turbo (мощная)" },  // ← добавили свою
];
```

Сохраните файл, перезагрузите приложение (Ctrl+C и `npm run dev`). Новая модель появится в выборе.

---

## Вопросы про настройки

### В: Как изменить таймаут загрузки URL?

**О:** В файле `.env`:
```
URL_FETCH_TIMEOUT_MS=15000
```

(время в миллисекундах)

---

### В: Как ограничить максимальный объём текста со страницы?

**О:** В файле `.env`:
```
URL_TEXT_LIMIT=16000
```

(количество символов)

---

## Примеры использования

### Пример 1: Генерация коротких описаний для маркетплейса

1. Загрузите CSV с товарами
2. Выберите колонки контекста: наименование, раздел, характеристики
3. Целевая колонка: "Описание для анонса" (короткое, до 255 символов)
4. Отключите HTML (обычный текст)
5. Модель: `gpt-5.4-nano`
6. Лимит строк: 10 (для теста)
7. Запустите

Результат: краткие, структурированные описания для маркетплейса.

---

### Пример 2: Генерация детальных описаний с HTML для сайта

1. Загрузите CSV с товарами
2. Выберите все доступные колонки (нажмите "Выбрать все")
3. Целевая колонка: "Детальное описание"
4. **Включите HTML** (галочка "Генерировать HTML")
5. Модель: `gpt-5.4-mini` или `gpt-5.4` (для качества)
6. Запустите

Результат: подробные описания с форматированием (жирный текст, списки, таблицы), готовые для вставки на сайт.

---

### Пример 3: Доповнение уже частично заполненного файла

1. Загрузите CSV, где некоторые описания уже заполнены
2. Отметьте чекбокс **"Пропускать заполненные"**
3. Запустите генерацию

Результат: пустые строки в колонке описания заполнены, уже написанные остаются нетронутыми.

---

### Пример 4: Использование данных со ссылок для контекста

1. Загрузите CSV, где есть колонка "Ссылка на спецификацию" (с URL)
2. Выберите эту колонку в "Колонки для контекста"
3. Запустите генерацию

Результат: приложение загрузит спецификации со ссылок и добавит информацию в контекст для каждого товара.

---

## Техническая информация

### Где хранится мой файл?

**О:** В памяти сервера (в процессе Node.js). После перезагрузки приложения все сессии удаляются. Это нормально для dev-режима.

Если понадобится production-версия с сохранением в БД — замените `lib/sessionStore.ts` на Redis/PostgreSQL.

---

### Отправляется ли мой API ключ на клиент?

**О:** Нет, никогда. API ключ читается только на сервере, в файле `.env`. На браузер не отправляется.

---

### Какие данные отправляются в OpenAI?

**О:** Для каждого товара отправляется:
- Наименование
- Раздел
- Выбранные контекстные колонки
- Загруженный со ссылок текст (если выбрали)

Остальное (например скрытые колонки, незагруженные поля) в OpenAI не идёт.

---

### Может ли приложение работать без интернета?

**О:** Нет, требуется соединение с OpenAI API (https://api.openai.com). Загрузка файла и выбор параметров работает оффлайн, но генерация требует интернета.

---

## Если ничего не помогло

1. Проверьте в браузере консоль ошибок (F12 → Console)
2. Посмотрите логи в терминале, где запущен `npm run dev`
3. Остановите сервер (Ctrl+C) и запустите заново
4. Убедитесь, что `.env` файл содержит правильный `OPENAI_API_KEY`

Если проблема остаётся — переустановите всё:
```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```
